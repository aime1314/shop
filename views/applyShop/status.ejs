<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/base.css">
    <script src="/javascripts/phone.js"></script>
    <style>
        .wrapper {
            background: #fff;
        }
        .top-area {
            position: absolute;
            bottom: 710px;
            left: 0;
            right: 0;
            text-align: center;
        }
        .top-area img {
            margin-bottom: 30px;
        }
        .top-area p {
            font-size: 28px;
            color: #2C2C2C;
        }
        .con-area {
            position: absolute;
            bottom: 410px;
            left: 0;
            right: 0;
            display: flex;
            justify-content: center;
        }
        .con-area p {
            font-size: 32px;
            color: #2C2C2C;
            margin-bottom: 28px;
        }
        .footer-area {
            position: absolute;
            bottom: 90px;
            left: 0;
            right: 0;
            display: flex;
            justify-content: center;
        }
        .footer-area .btn-primary {
            width: 300px;
            height: 90px;
            background: #3086DE;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 34px;
            color: #fff;
        }
        .footer-area .btn-default {
            width: 300px;
            height: 90px;
            border:1px solid #797A7A;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 34px;
            color: #797A7A;
        }
    </style>
</head>
<body>
    <div id="app">
        <div class="page">
            <div class="wrapper">
                <main class="content">
                    <% if(type == "sussess"){%>
                    <div class="top-area">
                        <img src="/images/icon_type_sussess.png">
                        <p>资料审核中</p>
                    </div>
                    <div class="con-area">
                        <p>请耐心等待审核结果</p>
                    </div>
                    <div class="footer-area">
                        <span class="btn-primary" onclick="window.location.href = 'clist'">返回</span>
                    </div>
                    <%}%>
                    <% if(type == "error"){%>
                    <div class="top-area">
                        <img src="/images/icon_type_error.png">
                        <p>申请失败</p>
                    </div>
                    <div class="con-area">
                        <p><%= msg %></p>
                    </div>
                    <div class="footer-area">
                        <span class="btn-default" onclick="window.location.href = 'regform?id='+ <%= shopId %> +'&type=review'">修改并提交</span>
                    </div>
                    <%}%>
                </main>
            </div>
        </div>
    </div>
</body>
</html>